# CV NPR inventory

## Known contents
- Vite + React source in `src/`
- Metadata assets in `public/`
- Build config in `vite.config.js` and `tailwind.config.js`

## Structural note
- This repo is an active source app, not an archive shell.
