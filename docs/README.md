# CV NPR project notes

## Purpose
Preserve the CV NPR application in a maintainable, source-first repository structure.

## Structure
- Keep the application source in `src/`.
- Keep public assets in `public/`.
- Keep maintenance and inventory notes in `docs/`.

## Maintenance
- Do not replace the app with a generic summary page.
- Keep build and deployment settings aligned with the Vite app.
